import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      // Temporarily removed all extensions
    },
  },
  plugins: [
    // Temporarily removed all plugins
  ],
}

export default config
